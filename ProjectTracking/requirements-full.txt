## Project dependencies for PTUD (Face Tracking System)
## Note: choose a Python 3.8+ interpreter

## Core AI / CV
opencv-python>=4.5.0
ultralytics>=8.0.0
deep-sort-realtime>=1.2.0
numpy>=1.21.0
Pillow>=8.0.0

## Backend / Web
Flask>=2.0.0
Flask-CORS>=3.0.0
SQLAlchemy>=1.4.0
Flask-SQLAlchemy>=2.5.0

## Utilities
python-dateutil>=2.8.0
requests>=2.25.0
matplotlib>=3.3.0
seaborn>=0.11.0
openpyxl>=3.1.0
python-dotenv>=0.19.0

## Document generation (used by helper scripts)
python-docx>=0.8.11

## Optional / manual-install (large binaries or CUDA-specific):
# PyTorch must be installed with the correct wheel for your platform and CUDA version.
# Examples (choose one that matches your system):
# CPU-only (example): pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA 11.7 (example): pip install torch --index-url https://download.pytorch.org/whl/cu117
# After installing torch, install facenet-pytorch:
# pip install facenet-pytorch
# We leave torch and facenet-pytorch commented here so pip -r won't attempt to pull large/incorrect wheels.
# torch
# facenet-pytorch

# End of file
